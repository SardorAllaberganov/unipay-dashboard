# AI_CONTEXT.md — UNIPAY Merchant Dashboard

> Snapshot of project state. Future Claude Code sessions read this first to bootstrap context.
> Source-of-truth files (always trust over this snapshot): [`STYLE_DISCIPLINE.md`](../STYLE_DISCIPLINE.md), [`src/types/domain.ts`](../src/types/domain.ts), [`docs/product_states.md`](../docs/product_states.md).

## Last updated
2026-05-12 — Reports-module polish round on top of the previous day's Prompt 8 ship. **ExportForm field spacing**: label-over-control blocks (dateRange / format / grouping) moved from `space-y-2` to `flex flex-col gap-2` — `space-y-*` applies `margin-top` to siblings, which doesn't apply to inline elements like the shadcn `<Label>`, so the visible gap collapsed to zero. The data-type `<fieldset>` keeps `space-y-2` (legend is block by default). **Data-type radio row**: bumped from `grid-cols-1 sm:grid-cols-2` to `grid-cols-1 sm:grid-cols-2 md:grid-cols-4` so Transactions/Students/Overdue/Payouts sit in one row on `md+`. **Department donut**: center label overflowed the hole — `formatUZS(298_436_322)` was wider than the 128px inner circle. Fix: `formatCompact` ("298,4 млн") + currency moved to the small line below ("Всего получено · UZS") + container `max-w-[7.5rem] truncate text-2xl md:text-3xl` clamp + `px-4 text-center` so the label is hole-confined. **Donut size**: `h-56` → `h-72` (224 → 288px), `innerRadius` 64 → 80, `outerRadius` 92 → 120; max-width clamp loosened to `max-w-[9.5rem]` after the hole grew to 160px. **Legend layout**: restructured from a centered wrapping chip row to a vertical list where each row is `flex items-center gap-3` with color dot + dept name on the left (`min-w-0 flex-1 truncate`) and amount right-aligned (`whitespace-nowrap tabular`). Long department names truncate; amounts always sit at a stable right edge. Lint clean (one `react-hooks/exhaustive-deps` warning earlier on `ByDayTable.items` was fixed by wrapping the initialization in `useMemo`). Verifications: tsc / lint / build all clean. Earlier:

2026-05-11 — **Prompt 8 (Reports module)** shipped end-to-end. New `/reports/*` nested layout with two tabs — `/reports/summary` (KPI row × 4 + 3 charts + by-day DataTable) and `/reports/export` (form + recent exports list). Tabs share a single canonical `?range=&from=&to=` URL state via a small `useReportRangeParam` hook so switching tabs preserves the active range (logged as a DECISION). Files: 8 components / 5 hooks / 1 layout / 2 pages / api.ts / schemas.ts / new MSW handler with 6 endpoints + 90-day deterministic per-day bucket fixture (mulberry32 seed `0x1558b0` matches the dashboard fixture for visual consistency). Auto-flips `processing` → `ready` on the export polling endpoint after 3s; `etaSeconds` decrements based on elapsed time. KpiCard/KpiSparkline **promoted** from `features/dashboard/components/` to `src/components/shared/` (now consumed by Dashboard + Reports — logged as a DECISION, same precedent as the Prompt 6 PanelStates promotion). Charts: revenue `BarChart` (granularity Tabs daily/weekly/monthly, all `fill="hsl(var(--brand-600))"`, `tick.fontSize:13`), channels horizontal `BarChart` (Y-axis labels via Recharts, sorted desc by amount), departments donut (6-slice brand+success+warning+info+slate palette via scale tokens cycling, `tabular font-mono` center label). ByDayTable: 6 columns (date / transactions / charged / commission / net / payout pill) — date + payout use `w-[1%] whitespace-nowrap` on both header and cell per the column-meta lesson; amount columns mirror `text-right whitespace-nowrap` on both sides; mobile gets `mobileCardRender` with a 2-col label/value grid. Export form: RHF + Zod factory; data type as RadioGroup of 4 styled cards; format as chip-toggle pair (CSV / NDJSON); grouping as a Select; includeContext as a Switch with a hint line. `<WriteButton>` on submit auto-disables offline; inline `Loader2` + "Готовим экспорт… ~N сек" status with `aria-live="polite"` while polling (never spinner-only). Recent exports list — Card list with file icon + filename + meta line (timestamp / data type / row count) + `<ExpiryCountdown>` ("истекает через N дн./час." with 1-minute self-ticker, "истёк" past-tense), Download `<Button>` and `<WriteButton variant="destructive">` per row, ConfirmDialog requires reason ≥20. All 6 states (loading skeletons / empty / error+retry / offline / partial banner / data) on every surface. New `reports.*` i18n namespace in both RU + UZ (~110 keys including ICU plural for payout count + rows count + expiry days/hours). Verifications: typecheck / lint --max-warnings 0 / build / §0.9 audit all clean (every `text-xs` in new code maps to allow-list: KpiCard category labels uppercase tracking-wider, payout pill chip/badge body). Earlier same day:

2026-05-11 — Build fix: extended `<StudentsTable>` `pagination` prop type with optional `onPageSizeChange` / `pageSizeOptions` to mirror `<DataTable>` `PaginationProps`. The Payments-module polish round had already wired the URL-synced per-page selector through `<StudentsListPage>` and the underlying `<DataTable>` already accepted the two optional fields, but `<StudentsTable>`'s local wrapper type was a stale subset (only `page / pageSize / total / onPageChange`) — `tsc -b` caught it. No runtime change; the props were already being forwarded verbatim via `pagination={pagination}` to `<DataTable>`. `npm run build` + `npm run lint` clean. Earlier same day:

2026-05-11 — Polish round 2 on the Payments module + several cross-cutting fixes that now propagate to every list page. **Pending table** thead alignment: amount header right-aligned; "Дата и время" + "Дней просрочки" headers get `whitespace-nowrap` so they don't wrap when the column collapses via `w-[1%]`. **Pending stats banner** rewritten as KPI-style stat columns (icon + uppercase label + `text-2xl md:text-3xl tabular` value) — 2 columns on Pending tab, 3 on Overdue. Horizontal dividers on mobile, vertical on desktop. **Per-page selector** now wired on Pending + Staff list pages (Students + Transactions already had it from the previous round) — same `[25, 50, 100]` URL-synced options with `pageSize` reset to page 1 on change. **Mobile pagination layout**: row 1 = showing-text + selector with `justify-content: space-between` and both items `whitespace-nowrap`; row 2 = pagination centered (`justify-center sm:justify-end`). Desktop unchanged. **Card-in-card eliminated on mobile** across all list surfaces: `PendingTable` + `RefundsTable` `mobileCardRender` switched from `<Card className="p-4">` root to plain `<div>` (DataTable's mobile path already wraps each row in `<Card>`); `RefundsPage` dropped the section-level `<Card>` wrappers in favor of plain `<section>` blocks with heading + description above each `<RefundsTable>`. **Receipt modal** sizes to content via `iframe.scrollHeight` measured on load — no trailing whitespace. Download-receipt button switched to default brand-primary variant. **i18n** added `common.pagination.*` keys (showing / pageOf / prev / next / rowsPerPage / goToPage), `common.actions.selectAll`, `payments.list.exportCsv|exportXlsx|exportPdf`, `payments.detail.previewReceipt`, `payments.detail.receipt.*` (21 receipt-iframe keys), and `payments.pending.stats.{studentsLabel,amountLabel,overdueLabel}`. **Lesson added** to LESSONS.md: DataTable column `meta` — `cellClassName: 'text-right'` does NOT propagate to the header; both sides need it explicitly. Narrow `w-[1%]` columns need `whitespace-nowrap` on the header. Values with digit-group spaces (UZS amounts, datetimes, mono ids with `…`) need `whitespace-nowrap` on cells. Earlier same-day:

2026-05-11 — **Prompt 7 (Payments module)** shipped end-to-end (4 routes — `/payments/transactions` list + `/:id` Pattern A detail / `/payments/pending` tabs / `/payments/refunds`), plus a long polish round (~15 iterations driven by user feedback on each surface). 32 new files in [`src/features/payments/`](../src/features/payments/) + shared promotions (`<BulkActionBar>` and `<FilterStack>` to `src/components/shared/`) + 2 new lib helpers (`errorCodes.ts` + `refundEligibility.ts`) + 9-endpoint MSW handler with 820 transaction fixtures and refund auto-approve. Domain extended with `FailureCode` × 4, `TransactionEvent`, `ManualPaymentMethod` × 3, `RefundReason` × 4, `RefundStatus` × 4, `Refund` interface, and 5 optional fields on `Transaction`. **DataTable upgrades** propagate to every table in the app: pagination now sits INSIDE the bordered table shell (no double divider); shadcn-style clickable page numbers (`< Назад · 1 · 2 · … · 12 · Далее >`) with active highlight + ellipsis; optional per-page selector (`Строк на стр. [50 ▼]`); new `bare` prop strips inner shell so a parent surface can own the chrome (used by TransactionsPage on desktop). All hardcoded RU strings in `<DataTablePagination>` replaced with `common.pagination.*` i18n keys. **Receipt** rendered via HTML srcDoc iframe styled as a real tuition receipt (header / payer + recipient / line items / totals / channel + status / transaction id / thanks + disclaimer) — gated behind a "Просмотр чека" button that opens a `<ReceiptPreviewDialog>`; iframe auto-sizes via `scrollHeight` measurement so the modal fits content. **Transaction timeline** rendered horizontally edge-to-edge with `h-[3px]` connector line + size-3.5 circles; past = `success-600`, failed = `destructive`, upcoming = `muted-foreground/20`. **Mobile** transactions: card-in-card eliminated (`<TransactionMobileCard>` now returns a plain `<div>`, matching `<StudentMobileCard>` pattern); pagination rendered as its own standalone card after the cards stack; outer shell dropped on mobile via `useIsMobile()` in TransactionsPage. **Two critical bugs fixed**: (1) `RefundDialog` + `RefundsTable` threw `Cannot mix BigInt and other types` because `Number(amount.amount / 100n)` is wrong — MSW collapses bigint → number on the wire so the runtime value is already a number. Fix: `Number(amount.amount) / 100` works for both forms. New lesson logged. (2) Transactions page had document-level over-scroll past content (html.scrollHeight = 4262 vs viewport 800). Architectural fix: `html, body { height: 100%; overflow: hidden; }` in [`globals.css`](../src/styles/globals.css) — locks the SPA app shell so main owns all scrolling. Standard pattern. New lesson logged. Also: export button now a brand-primary `<DropdownMenu>` with CSV/Excel/PDF formats (icons + i18n labels), download-receipt button switched to default brand variant, row click on tx list navigates DIRECTLY to detail page (intermediate Sheet flow + `TransactionDetailSheet` component dropped per user feedback), kebab column dropped (row click + detail page own all actions), amount column headers right-aligned in both Transactions and Students tables (`headerClassName: 'text-right'` + `whitespace-nowrap` on cells so values don't break at digit-group spaces). Earlier same-day work below.

2026-05-11 — `<DataTable>` now supports per-row cell-merging via `meta.cellColSpan: (row) => number`. The TanStack `ColumnMeta` augmentation in [`src/components/shared/DataTable.tsx`](../src/components/shared/DataTable.tsx) gained a new field, and the body row renderer walks cells with a `skip` counter so that when a cell sets `cellColSpan > 1` for a given row, the subsequent N-1 cells are skipped and the rendered `<TableCell>` gets a `colSpan` attribute. First consumer: the **staff list** ([`StaffTable.tsx`](../src/features/staff/components/list/StaffTable.tsx)) merges the `status + actions` tail cells for pending rows so the "Ожидает" badge and kebab share a single right-aligned cell (badge on the left of the merged cell, kebab on the right via `flex justify-between`). Non-pending rows keep the standard 6-column layout. Earlier the same day: second round of Students-module polish after the user reviewed each surface:

1. **Edit Student converted from `<ResponsiveSheet>` to a standalone page** at `/students/:id/edit`. Per-spec lesson says "Edit stays in Sheets" but the user wanted consistency with Add (which is a page), since the form has the same Personal + Academic field set. Registered as a sibling of `/students/:id` so the detail page's tabs/action bar don't render. [`EditStudentPage.tsx`](../src/features/students/pages/EditStudentPage.tsx) uses Pattern A (back-link → identity title → two `<Card>` sections → fixed-bottom action bar with Cancel ghost left, Save WriteButton right). `<EditStudentSheet>` deleted. `<StudentDetailActionBar>` no longer takes `onOpenEdit` — Edit button navigates directly to the new route.
2. **Action bar layout aligned with the "group by intent" pattern** in both Add and Detail. AddStudentPage: Cancel ghost LEFT, [Save & add another, Save & close] cluster RIGHT — with `md:justify-between`. StudentDetailActionBar: [Edit, Send SMS] (maintenance) LEFT, [Deactivate, Delete] (destructive) RIGHT. Both action bars: removed `mx-auto` + `max-w-{2xl,5xl}` inner constraints — bars span the full content width within the sidebar offset. Mobile: each intent group on its own row with `flex-1` 50/50 splits.
3. **Form sections wrapped in `<Card>`** on Add Student (PersonalInfoSection / AcademicInfoSection / PaymentSetupSection) so the three groups read as visually distinct boxes instead of merging into one continuous form.
4. **Student Profile horizontal overflow fix on mobile.** Root cause: the page's grid `<aside>` and `<section>` had no `min-w-0`, so grid tracks default to `min-width: auto` (intrinsic content min-width) and the wide DataTable inside the Tabs section pushed the whole grid past the viewport. Added `min-w-0` on the outer wrapper + aside + section. Also added `break-words` / `break-all` on header text and `min-w-0 break-words` on `<dl>` `<dd>` values so long emails / dept paths / mono studentIds wrap inside their rows. ProfileLeftColumn's Row component now stacks **label-above-value on mobile** (small uppercase tracking-wider `text-xs` definition label per §0.2 allow-list) and switches to horizontal label-left/value-right on `md+`.
5. **TransactionsTab mobile card render** added. The DataTable was falling back to horizontal-scroll table on mobile (no `mobileCardRender`), which made transaction history unusable on narrow viewports. New `<TransactionMobileCard>` renders date+amount on top row (amount `text-base font-semibold` — loudest), channel + status badges below, receipt link `ml-auto` when present. Click still opens `<TransactionDetailSheet>` via the existing `onRowClick` wiring.
6. **Native date-input fixes.** `<input type="date">` inherits `display: flex` from shadcn's base Input class, which packed the native shadow children (date editor + calendar indicator) flush at the start with no gap. Added `justify-content: space-between` to `input[type='date' | 'datetime-local' | 'month' | 'time' | 'week']` in [`globals.css`](../src/styles/globals.css). Also styled `::-webkit-calendar-picker-indicator` with `margin-left: auto` + visible muted-foreground tone (`opacity: 0.6` resting / `1` hover; `filter: invert()` adapts per theme) so the picker trigger is no longer a tiny invisible UA glyph.
7. **DateRangePicker triggers: icon moved to right.** Dashboard, Student Activity tab, Staff Activity tab — all three now render `<span>{label}</span><Icon />` with `justify-between` instead of icon-left. Activity tabs also gained `useDateRangeLabel(range)` so the trigger button shows the active range (`Последние 30 дней` / `DD.MM.YYYY – DD.MM.YYYY`) instead of the static "Date range" placeholder.
8. **Students filter bar settled on `<FilterStack>` (vertical labeled groups)** after two earlier iterations (inline labels, bordered containers). Each group renders a small uppercase tracking-wider label above its chips/picker. Chips upgraded to read as buttons: `border border-border bg-card text-foreground` inactive, `border-brand-600 bg-brand-50 text-brand-700` active. The bare-number year chips are now unambiguous under the "КУРС" label.
9. **`<StudentMobileCard>` glanceability rewrite** — killed the 4-row `<dl>` grid. New shape: identity row (checkbox + sm avatar + name + studentId mono + kebab) → status LEFT + balance RIGHT (`text-base font-semibold` for the amount) → dept · year subtitle (single muted truncating line) → "Последний платёж: {date}" inline prose.
10. **`<DialogFooter>` mobile button gap fix.** shadcn primitive used `sm:space-x-2` which left mobile (flex-col-reverse) with zero gap between stacked buttons. Swapped to `gap-2` so spacing works on both axes. Affects every Dialog/ConfirmDialog in the app.
11. **`<DialogHeader>` text alignment locked to `text-left`** on every viewport. Default was `text-center sm:text-left`; ConfirmDialog wraps the title in a `flex items-center gap-2` (so an AlertTriangle can precede destructive titles), and that flex container forced its content to left-align while the sibling DialogDescription still inherited the mobile `text-center` — title left, description centered = mismatched. Now both consistently left-aligned.
12. **Top action buttons (Import + Add) split 50/50 on mobile** in `<StudentsListPage>` via a `flex w-full gap-2 md:w-auto` wrapper passed into PageHeader actions; each button `flex-1 md:flex-none`.
13. **Add-schedule-row MSW handler bug fix.** `addEmptyRow` was POSTing `period: ''` which the MSW handler rejected with `400 invalid_input` (period is a required, truthy field). Now sends `t('students.schedule.newRowPeriod')` = "Новый период" / "Yangi davr" as a localized placeholder the user can immediately inline-edit.

Earlier the same day: **post-Prompt-6 polish + bug fixes:** (1) **MSW responses with `Money.amount: bigint` were silently failing** because `HttpResponse.json` → `JSON.stringify` throws on raw bigint. Patched `BigInt.prototype.toJSON` globally in [`src/main.tsx`](../src/main.tsx) to serialize as a JSON number (UZS tiyins fit in `Number.MAX_SAFE_INTEGER`; `formatMoney` already handles both bigint and number, so no consumer changes needed). (2) **`<ErrorState>` and `<OfflineState>` referenced stale i18n keys** (`states.errorTitle` / `states.errorDescription` / `states.offlineTitle` / `states.offlineDescription` / `common.retry`) that don't exist — i18next fell back to rendering the key paths verbatim, masking the underlying serialization bug. Fixed to use canonical `common.states.errorTitle` / `common.states.errorBody` / `system.offline.title` / `system.offline.body` / `common.actions.retry`. (3) **Students filter UX iterated 3 times** before landing on the final shape: bare-number year chips were unreadable when laid out inline → tried inline labels → tried bordered per-group containers → settled on vertical `<FilterStack>` (small uppercase tracking-wider label above chips) matching the mobile bottom-Sheet structure. Chips also got upgraded visuals: inactive `border-border bg-card text-foreground` (now read as pressable buttons, not loose muted text), active `border-brand-600 bg-brand-50 text-brand-700` (confident pressed state). Department picker simplified back to a plain `<Button variant="outline">` showing "Все" or "{count} active" summary, since the `<FilterStack>` carries the "ОТДЕЛЕНИЕ" label. (4) **`<StudentMobileCard>` restructured** for glanceability: killed the `<dl>` grid (sparse, 4 rows of small labels); new shape = identity row (checkbox+avatar+name+kebab) → status LEFT + balance RIGHT (`text-base font-semibold` — most prominent on the card) → dept·year subtitle (single muted line, truncates) → last-payment recency as inline prose. Avatar dropped to `size="sm"`; kebab uses `-mr-1` to sit flush against the Card's right padding. (5) **Top action buttons (Import + Add) split 50/50 on mobile** in `<StudentsListPage>` via a `flex w-full gap-2 md:w-auto` wrapper inside the PageHeader actions slot — each button `flex-1 md:flex-none`. Earlier the same day: **Prompt 6 (Students module)** shipped end-to-end: 5 routes (`/students` list with bulk-select + filter URL-sync · `/students/new` Pattern A · `/students/import` 4-step wizard · `/students/schedules` template card list · `/students/:id` Pattern A detail with 4 tabs). 24-endpoint MSW handler with deterministic 200+ student seed across the existing 111-node department tree, schedule rows (2–3 per student), transactions (0–5), notes (0–2), activity log (3–6), 3 pre-seeded schedule templates, import-session in-memory Map with per-row validation. **Pre-batch fix:** staff list kebab alignment — extended `<DataTable>` with `ColumnMeta.headerClassName` / `cellClassName` (TanStack module augmentation) so actions columns collapse to content via `w-[1%]`; applied to staff + every Students table. **Foundation pass:** promoted `PanelStates` from `features/dashboard/components/` to `src/components/shared/` (resolves carry-over; 9 importers repointed); built generic `<TreePicker>` in `src/components/shared/` consumed in 4 spots (list filter / Add Student / Apply Template / Change Department); wired `<WriteButton>` offline tooltip to `t('common.offline.tooltip')`; added `xlsx@0.18.5` (dynamic-import only — ships as own 142 KB gzip chunk for template + error-report download). **Domain:** new `StudentPaymentStatus = 'paid'|'partial'|'pending'|'overdue'` (distinct from transaction `PaymentStatus`), `PaymentType`, `ScheduleRow` + `ScheduleRowStatus`, `ScheduleTemplate`, `StudentNote`, 15-action `StudentActivityAction` + entry, `ImportRow` + `ImportRowError` + `ImportSession`. `StatusBadge` got a `'partial'` variant (`CircleDot` icon + `info` tone). **List page:** URL-synced filters (search 200ms debounce / dept TreePicker multi via Popover / year chips 1-6 / payment status chips / edu-type chips; collapsible on mobile via bottom Sheet), 50-per-page pagination, bulk select with `<BulkActionBar>` (Pattern A class string, only renders when rows selected, `pb-28` on wrapper conditional). **Detail page:** Pattern A (back-link / identity row / chips / `pb-28` / fixed-bottom `<StudentDetailActionBar>` with Owner-only Delete behind tooltip-gated disabled Button), 2-col layout = Personal/Academic cards left + 4 underline-style tabs right: Schedule (new `<InlineEditCell>` primitive — `text-sm` minimum / Enter saves / Esc cancels / blur commits; Apply Template flow with reason ≥20), Transactions (DataTable + `<TransactionDetailSheet>` right-Sheet ≥md / bottom <md), Notes (append-only timeline + Add note; audit trail noted in copy), Activity (action Select + DateRangePicker + paginated DataTable). **Add Student:** standalone Pattern A page, 3 sections (Personal · Academic · Payment Setup with Switch template/inline rows), async studentId uniqueness via `useCheckStudentId` (debounced 350ms), save-and-add-another preserves academic dropdowns. **Import wizard:** internal step state (Download / Upload / Review / Confirm) with inline step indicator; Step 1 dynamic-imports `xlsx` and generates a 2-sheet template (Students + Instructions); Step 2 drag-drop file with 5 MB / 5 000-row guards; Step 3 inline-editable review table with PATCH on blur/Enter (errors as `bg-destructive/10` cells with field-error message below, never as toast); Step 4 requires reason ≥20 when imported count > 100. **Templates page:** card list of 3 pre-seeded templates with Edit/Duplicate/Apply/Delete; Apply opens a confirm with reason ≥20 + live preview count (debounced via `studentsApi.list` total). All 6 states per data surface. typecheck / lint --max-warnings 0 / build all clean. Earlier same day: **Horizontal-padding alignment** locked in. Earlier the same day: **Prompt 5 (Staff Members module)** shipped in two passes: first a 4-tab list+detail with quick-view Sheet (since deleted), then a full rebuild per the second spec — no Sheet, list rows navigate to `/staff/:id`, 2-column → single-column detail with full identity merged into Profile tab, 4 tabs (Profile / Role & Permissions / Activity / Sessions), 9 dialogs (incl. 2-step DeleteAccount with email-confirm + 2-step TransferOwnership with TRANSFER-phrase-confirm), permissions matrix (`6 resources × {read, write, destructive}` driven by static `ROLE_PERMISSIONS` map in domain.ts), cross-tab sessions sync via `storage` event on `unipay-staff-sessions-sync` key. **MSW handler** `mocks/handlers/staff.ts` has 15 endpoints, ~180 activity entries w/ ip+device, 10 sessions across active members, fixtures of 1 owner + 2 finance + 3 operators + 1 viewer + 2 pending invites. **`<DataTable>` extended** with `rowHref` (cmd/middle/keyboard click support, SPA `navigate()` w/ optional state) + `onRowClick` + `rowClassName` so staff list rows are semantic links. **`<DetailPageSkeleton>`** shared primitive added at `src/components/shared/` (§0.5-shaped loading skeleton, configurable via `avatar`/`chips`/`tabs` props). **`<ConfirmDialog>` default-label bug** fixed — fallbacks were `t('common.cancel')` / `t('common.confirm')` which return the literal keys (real keys live at `common.actions.cancel` / `common.actions.confirm`). **Departments drag-drop UX overhaul**: split sensors (`MouseSensor distance:4` + `TouchSensor delay:250 tolerance:8`), `<DragOverlay>` portaled to `document.body` (zIndex 40 → below Radix Dialog z-50), source row dims to `opacity: 0.4` and stays put (no layout shift), root drop banner absolutely-positioned over the top of the scroll area (zero shift on drag start), `closestCenter` + `MeasuringStrategy.Always` so root-drop reliably registers. **EditRoleDialog rebuilt** as single-screen RadioGroup w/ inline role descriptions + live permission diff + always-visible reason (was an awkward 2-step). **EditAccessDialog** got an explicit "Full access vs Specific" mode toggle so the `empty = all` rule isn't buried in a hint. **EditContactDialog removed** — email/phone editing folded into ProfileTab's edit form (one canonical edit surface for the profile). Tab visual on `/staff/:id` matches `<OrgTabsNav>` (underline tabs, not segment pills) — class strings centralized in `TAB_LIST_CLASS` / `TAB_TRIGGER_CLASS` constants. **Sticky removal lesson** logged — filter bars, page-level toolbars, anything-on-scroll-headers: stop pinning with `position: sticky`. Only sanctioned exception is `<OnboardingLayout>`'s step indicator. Earlier the same day: deployed demo auth fix (see below).

Deployed demo auth fix: dropped the `import.meta.env.DEV` gate around MSW startup in [`src/main.tsx`](../src/main.tsx) and passed `serviceWorker: { url: \`${import.meta.env.BASE_URL}mockServiceWorker.js\` }` to `worker.start()` so the worker resolves under both `/` (dev) and `/unipay-dashboard/` (GitHub Pages). Sign-in form ([`SignInForm.tsx`](../src/features/auth/components/SignInForm.tsx)) now ships `owner@unipay.dev` / `demo1234` as `defaultValues` unconditionally — `import.meta.env.DEV ? prefilled : empty` collapsed to the prefilled branch in all environments. Net: the deployed Pages demo has a working sign-in for the first time. MSW bundle (~97 KB gzipped `browser-*.js` chunk) now ships in prod too; gate this behind a `VITE_USE_MOCKS` env var when the real Express+Mongo backend lands. Earlier the same day: Bank Edit Sheet polish (see below).

Bank Edit Sheet polish: `BankImmutableSummary` restructured from a 2-column `dl grid` to stacked groups (label `text-xs uppercase tracking-wider` on top, value `text-sm font-medium` below; `break-all` on the 20-digit account number so it wraps inside narrow sheets) and each row is now a tap-to-copy `<button>` with `navigator.clipboard.writeText` (+ hidden-textarea `execCommand` fallback for non-secure contexts), feature-detected `navigator.vibrate(10)` haptic, `Copy → Check` icon swap for 1500ms, sonner toast, and an `sr-only role="status" aria-live="polite"` announcement. New i18n key `organization.bankAccounts.copyErrorToast` (RU + UZ). Earlier the same day: Prompt 4 (Organization module) — 4 sub-routes under `/organization/*` (Profile / Departments / Bank Accounts / Branding) via nested `<OrganizationLayout />` + horizontal-scroll `OrgTabsNav`; full MSW handler with 16 endpoints + 111-node tree fixture (3 faculties × 4 depts × 2 years × 3 groups) + 5s server-side bank-verification flip; **add flows are standalone pages** (`/organization/bank-accounts/new`, `/organization/departments/new[?parentId=X]`) with §0.5 Pattern A action bar — sibling of `OrganizationLayout` so the tabs strip doesn't render on them. `LogoUploader` / `ColorPicker` / `ReceiptPreview` promoted to `src/components/shared/` with parameterized `labels` prop. Onboarding still uses them. `Organization`, `Branding`, `LegalForm`, `OrgType` added to `src/types/domain.ts`. Departments tree uses `@dnd-kit/core` with delay-based touch activation (long-press starts drag), cycle-prevention via `descendantIds` banned-set, reparent-confirm with `requireReason` when affected students > 50, cascade-delete with reason ≥20 when children exist. **Mobile fixes:** action icons hidden on mobile (tap-row → bottom Sheet), `min-h-0` chain wired through `ResponsiveSheet` + `DepartmentDetailPanel` + `DepartmentTree` so `overflow-y-auto` actually scrolls; focus-ring clipping fixed via `-mx-1 px-1` outset on all internal scroll wrappers (overflow-y-auto implicitly forces overflow-x: auto per CSS spec, which clips the input's `ring-offset-2` halo). Earlier the same day: Dashboard polish (KPI hero responsive, ZhiPay DateRangePicker), Onboarding Skip Setup, Prompt 3 Dashboard Home.

## What this app is
**UNIPAY** — merchant dashboard for Uzbek educational institutions (universities, schools, kindergartens) to manage tuition payments. Locale: RU primary, UZ secondary (Latin). Currency: UZS (space separator). Timezone: Asia/Tashkent.

Audience: **Owner / Finance Manager / Operator / Viewer** (`Role` in [`src/types/domain.ts`](../src/types/domain.ts)).

Mobile-first with full feature parity at 320px+ — no read-only mobile, no use-desktop banner.

## Stack snapshot
React 18 · Vite 5 · TypeScript 5 (strict + `noUncheckedIndexedAccess`) · Tailwind v3 · shadcn/ui · React Router v6 (**HashRouter**) · TanStack Query v5 · React Hook Form + Zod · TanStack Table v8 · Recharts · react-i18next · date-fns 3 · MSW v2 · lucide-react · sonner · @fontsource/inter + @fontsource/jetbrains-mono · cmdk · canvas-confetti (dynamic import for the Step 5 finish flourish) · @dnd-kit/core + @dnd-kit/sortable + @dnd-kit/utilities (Organization departments tree drag/drop).

## Token system
- **HSL scales**: `brand-50…950` (anchor `#1558B0` ≈ brand-600), `slate-50…950`, plus 3-stop `success/warning/danger/info` ramps.
- shadcn semantic tokens (`--background`, `--foreground`, `--card`, `--muted`, `--border`, `--ring`) **map onto scales**, never raw HSL.
- Legacy semantic aliases (`--primary-light`, `--success-light`, `--surface-2`, `--refund`, etc.) kept as a backward-compat layer pointing into scale vars.
- Motion: `--duration-fast/base/slow` (120/180/240ms), `--ease-standard/emphasized`. `prefers-reduced-motion` global override.
- Density: `--row-h` driven by `html[data-density]` (40px compact / 44px comfortable).
- Tabular numerals: `html[data-tabular-nums="true"]`.
- Radius scale: `--radius: 0.5rem` (8px) drives `rounded-md` (6px) / `rounded-lg` (8px) / `rounded-sm` (4px). Card primitive uses Tailwind-default `rounded-xl` (12px, outside the cascade) for a slightly softer card edge; h-9 form controls (Button / Input / Select trigger / Textarea / Toggle / Topbar search) all use `rounded-lg` (8px). Hierarchy reads: Card 12px > controls 8px > internal items 4px.

## Folder map
```
src/
├── App.tsx                       # HashRouter + ThemeProvider + MSW + boot calls
├── main.tsx
├── router.tsx                    # MaintenanceGate + PathAwareAuthGuard + system preview routes
├── components/
│   ├── auth/    AuthLayout       # Full-bleed chrome, owns its own TooltipProvider
│   ├── layout/                   # AppShell, AppShellContext, Sidebar, Topbar, UserMenu,
│   │                             # CommandPalette, HelpOverlay, ThemeToggle, NotificationsBell,
│   │                             # UnipayLogo, shortcuts.ts
│   ├── system/                   # SystemStateLayout, SystemErrorBoundary, NotFound/ServerError/
│   │                             # Forbidden/Maintenance/Offline + OfflineBanner
│   ├── unipay/                   # Money, MaskedAccount, WriteButton, KeyboardHint
│   │                             # + index.ts re-exports of badges from shared/
│   ├── ui/                       # shadcn primitives + Kbd
│   └── shared/                   # DataTable, badges, BackLink, ConfirmDialog, ResponsiveSheet,
│                                 # DateRangePicker, EmptyState/ErrorState/OfflineState/PartialBanner,
│                                 # LogoUploader, ColorPicker, ReceiptPreview (promoted from onboarding)
├── hooks/                        # useNetworkState, useKeyboardShortcuts, useMediaQuery, …
├── lib/
│   ├── auth.ts                   # useSyncExternalStore + sessionStorage + idle timeout
│   ├── preferences.ts            # bootPreferences applies data-density + data-tabular-nums
│   ├── maintenanceState.ts       # bootMaintenanceFromUrl reads ?maintenance=on|off
│   ├── referenceId.ts            # 8a7c-2f1e shaped error ids
│   ├── systemEvents.ts           # logPageError → reference id
│   ├── tone.ts                   # tone-to-class map (single source of truth)
│   ├── format.ts                 # bigint-aware formatMoney, masking
│   └── i18n/locales/{ru,uz}.json
├── pages/                        # Placeholder (Dashboard now in features/dashboard/)
├── features/
│   ├── auth/                     # SignIn / ForgotPassword / ResetPassword
│   │   ├── pages/                # SignInPage, ForgotPasswordPage, ResetPasswordPage
│   │   ├── components/           # SignInForm, PasswordField, LockedAlert, DevRoleSwitcher
│   │   ├── hooks/                # useFailedAttempts, useForgotPassword, useResetPassword
│   │   ├── api.ts                # forgot + reset fetch wrappers (signIn lives in lib/auth.ts)
│   │   └── schemas.ts
│   ├── dashboard/                # Home page — KPIs + charts + recent activity panels
│   │   ├── pages/                # DashboardPage (URL date-range sync via ?from=&to=)
│   │   ├── components/           # KpiSparkline, KpiCard, KpiRow, RevenueChart,
│   │   │                         # PaymentStatusChart, RecentTransactions, UnpaidStudents,
│   │   │                         # GreetingTitle, PanelStates (6-state primitives)
│   │   ├── hooks/                # useDashboardSummary, useRevenueSeries,
│   │   │                         # usePaymentStatusBreakdown, useRecentTransactions,
│   │   │                         # useUnpaidStudents, useBulkRemindUnpaid
│   │   └── api.ts                # typed fetch wrappers + ResponseMeta shape
│   ├── organization/             # /organization/* — Profile / Departments / Bank Accounts / Branding
│   │   ├── pages/                # OrganizationLayout, ProfilePage, DepartmentsPage,
│   │   │                         # BankAccountsPage, BrandingPage,
│   │   │                         # AddBankAccountPage, AddDepartmentPage (full-page add flows)
│   │   ├── components/           # OrgTabsNav, DepartmentTree, DepartmentNode,
│   │   │                         # DepartmentDetailPanel, BankAccountCard,
│   │   │                         # BankAccountForm (edit Sheet), BankAccountFormParts (shared pickers)
│   │   ├── hooks/                # useOrganization, useUpdateOrganization,
│   │   │                         # useDepartments (flat-list + buildTree + descendantIds),
│   │   │                         # useDepartmentMutations (create/update/delete/move),
│   │   │                         # useBankAccounts, useBankAccountMutations,
│   │   │                         # useBranding, useUpdateBranding
│   │   ├── schemas.ts            # profileSchema · brandingSchema · bankAccountSchema · departmentSchema
│   │   └── api.ts                # typed fetch wrappers for all org endpoints
│   └── onboarding/               # 5-step wizard for new institution accounts
│       ├── pages/                # OnboardingPage (router + provider) + Step1..Step5
│       ├── components/           # OnboardingLayout, StepIndicator, StepActionBar,
│       │                         # PhoneInput, ColorPicker, LogoUploader, ReceiptPreview,
│       │                         # BankCombobox, BankAccountFields, DepartmentTreeEditor,
│       │                         # InviteStaffFields
│       ├── context/              # OnboardingContext (local feature state per §0.12)
│       ├── hooks/                # useOnboardingDraft (query + mutation),
│       │                         # useOnboardingComplete, useOnboardingGuard,
│       │                         # useOfflineDraftQueue (localStorage queue + flush)
│       ├── fixtures/             # uzBanks (20 banks + MFO), uzRegions (14)
│       ├── api.ts                # draft + complete + templates fetch wrappers
│       └── schemas.ts            # 5 t-aware Zod factories + DepartmentNode + OnboardingDraft
├── providers/ThemeProvider.tsx
├── styles/globals.css
├── types/domain.ts               # Role finance_manager · Money bigint · Tone · Locale · StatusDomain
└── mocks/                        # MSW worker + handlers
```

## Module-level state stores (§0.12)
All four use `useSyncExternalStore` with module-level cache + listener set + `storage` event cross-tab sync (where relevant):
- [`lib/preferences.ts`](../src/lib/preferences.ts) — theme/density/language/timezone/date_format/time_format/tabular_numerals. DOM side effects on `<html>`. Boot: `bootPreferences()` from `App.tsx`.
- [`lib/maintenanceState.ts`](../src/lib/maintenanceState.ts) — `{ active, startedAt, estimatedEndAt }`. URL boot via `bootMaintenanceFromUrl()` reads `?maintenance=on|off`, applies, strips param.
- [`lib/auth.ts`](../src/lib/auth.ts) — session backed by `sessionStorage` (`unipay-session`). `signIn(email, password)` is now **async**: hits `/api/auth/sign-in` via MSW, parses user + JWT-shaped token, sets session. `signInAsRole(role)` stays sync for the dev role switcher. `signOut({ reason })` writes a `unipay-signout-reason` flag that `SignInPage` reads + clears to surface the expired banner. `updateUser(patch)` patches `cached.profile` in-place — used by the onboarding finish flow to flip `onboardingComplete: true`. `useIdleTimeout()` returns `'idle'` after 30 min of mousedown/keydown/scroll/touchstart silence.
- [`features/auth/hooks/useFailedAttempts.ts`](../src/features/auth/hooks/useFailedAttempts.ts) — `{ count, firstFailureAt }` in `sessionStorage` (`unipay-auth-failed-attempts`). 5-attempt / 15-min sliding window. `recordFailure` / `recordSuccess` mutate; `isLockedOut(state)` and `getLockoutRemainingMs(state)` derive in consumers. `LockedAlert` ticks 1s while inside a window.

## Routing
HashRouter so the app deploys to any static host without a 404.html shim.
- `MaintenanceGate` outermost — `?maintenance=on` flips it; `/system/preview/*` bypasses.
- Auth pages live **outside** `<AppShell>` as siblings of the catch-all guard: `/sign-in`, `/forgot-password`, `/reset-password`. All wrap in `<AuthLayout>`.
- `PathAwareAuthGuard` — unknown deep-link before sign-in → full-bleed 404; known → redirect to `/sign-in?next=…`. `isKnownPath` whitelists `/`, `/sign-in`, `/forgot-password`, `/reset-password`, plus the in-shell route prefixes (now including `/onboarding`).
- `AuthGuard` wraps everything in `<AppShell>`; idle → `signOut({ reason: 'session_expired' })`. The reason is read by `SignInPage` (sessionStorage flag) to show the expired banner; `?expired=1` is also honored.
- `OnboardingGuardWrapper` wraps `<AppRoutes>` and runs `useOnboardingGuard`: when `session.profile.onboardingComplete === false` and current path is not `/onboarding/*`, redirects to `/onboarding/1`.
- `/onboarding/:step` is registered inside `AppRoutes` so it lives inside `<AppShell>`. The user identity / chrome persists; the wizard's own `OnboardingLayout` adds sticky step indicator + fixed action bar (Pattern A from §0.5).
- `/` mounts `<DashboardPage />` from `features/dashboard/` (canonical home — no parallel `/dashboard`, see DECISIONS 2026-05-11). Sign-in success and post-onboarding completion both land here.
- `SystemErrorBoundary` wraps `<AppRoutes>`; thrown errors land on `<ServerErrorState>` with copy-on-click reference id.
- Preview routes for QA: `/system/preview/{404,500,403,offline,maintenance}`.

## Layout shell contract
- `flex h-dvh bg-background` root with `--sidebar-width` set as inline style on the shell wrapper.
- Skip-link first child; `<main id="main-content" tabIndex={-1}>` is its target.
- `<TooltipProvider>` outermost so portal tooltips from Sheet contents resolve.
- Right column has `min-w-0` so flex children can truncate.
- AppShell auto-collapses sidebar at `(max-width: 1023px)`.
- `<main>` padding is **conditional on `onboardingActive`**: default `p-4 md:p-6`, but during onboarding switches to `px-4 pb-4 md:px-6 md:pb-6` (zero top padding) so the wizard's sticky step indicator butts directly against TopBar with no gap. `<main>` is the scroll container (`overflow-y-auto`), so sticky elements inside use `top-0` relative to it (NOT a viewport offset).

## Sidebar
- 64↔240 width, `transition-[width] duration-base ease-standard`.
- Items `h-9 gap-2.5 px-3`, icons `size-4`. Active state: `bg-brand-50 text-brand-700` + `before:` 2px accent stripe (dark-mode mapping wired).
- Sections from spec §13: Main / Organization / Students / Payments / Finance / System.

## Power-user surfaces
- `⌘K` / `Ctrl+K` toggles Command Palette ([CommandPalette.tsx](../src/components/layout/CommandPalette.tsx)).
- `?` opens Help Overlay ([HelpOverlay.tsx](../src/components/layout/HelpOverlay.tsx)).
- `t` toggles theme; `/` focuses search; `Esc` closes overlays.
- `g {x}` chord patterns documented in `shortcuts.ts`. Currently visualized in HelpOverlay; chord listener implementation lands in a later pass.

## Notifications bell
- [NotificationsBell.tsx](../src/components/layout/NotificationsBell.tsx) takes `unreadCount?: number` (default `0`). Red dot only renders when > 0; empty state otherwise.
- Localized via `notifications.title` / `notifications.empty` / `notifications.unreadCount` (RU + UZ). When unread, `aria-label` appends `unreadCount` so screen readers announce the badge.
- Trigger has no real notifications source yet — Topbar passes nothing → no dot visible.

## Branding & deploy
- Favicon: [public/favicon.svg](../public/favicon.svg) — UP mark on brand-600 rounded square; matches [UnipayLogo](../src/components/layout/UnipayLogo.tsx). Paired with `<meta name="theme-color" content="#1558B0">` in [index.html](../index.html).
- GitHub Pages CI: [.nojekyll](../.nojekyll) at repo root + [public/.nojekyll](../public/.nojekyll) (Vite copies into `dist/`) bypass the auto Jekyll build that choked on `{{ }}` JS literals in `STYLE_DISCIPLINE.md` code blocks. Repo-side: Settings → Pages → Source must be set to "GitHub Actions" so [deploy.yml](../.github/workflows/deploy.yml) is the canonical deploy path.

## System states catalog (§0.11)
- `SystemStateLayout` with `in-shell` / `full-bleed` variants.
- 5 states + OfflineBanner.
- ServerError carries copy-on-click reference id; `lib/systemEvents.logPageError()` generates and logs.
- AuthLayout owns its own `<TooltipProvider>` because it lives outside `<AppShell>`.

## Domain primitives
- `Money` is **bigint** (minor units — UZS in tiyins, USD in cents). `formatMoney()` divides by 100 at display time. Negatives render `text-danger-600` with `−` prefix.
- `MaskedAccount` shows `••••0123`; never reconstructs full PAN.
- `WriteButton` disables when `useNetworkState() === false` and shows a tooltip.
- `KeyboardHint` re-exports the canonical Kbd primitive.

## DateRangePicker (shared)
- [`src/components/shared/DateRangePicker.tsx`](../src/components/shared/DateRangePicker.tsx) + helpers [`src/components/shared/dateRange.ts`](../src/components/shared/dateRange.ts) — ZhiPay-format range picker: quick-select sidebar (Today / Yesterday / Last 7 days / Last 30 days / Custom) on the left, two-month calendar with custom prev/next chevron header on the right, footer with resolved `from – to` summary + Cancel / Apply. Selections are pending until Apply commits.
- **API.** `DateRangeValue = { range: 'today' | 'yesterday' | '7d' | '30d' | 'custom', customFrom?, customTo? }`. Helpers: `resolveDateRange(value)` → concrete `{ from, to }`; `useDateRangeLabel(value)` → localized trigger label.
- **Trigger.** Passed as `children` via `asChild` so consumers control the visual. Inline the `<Button>` directly — wrapping in a function component breaks Radix `Slot`'s ability to forward ref/onClick (caught in this session: the picker silently didn't open until the trigger was inlined).
- **Mobile.** `matchMedia('(max-width: 767px)')` collapses the calendar to a single month and stacks the sidebar above. Popover content is clamped to `w-[min(860px,calc(100vw-1rem))]` + `collisionPadding={8}` so Radix shifts off the viewport edge near the right border.
- **i18n.** `common.daterange.{title,quick-select,today,yesterday,7d,30d,custom,cancel,apply,prevMonth,nextMonth}` in `ru.json` + `uz.json`. The Calendar's built-in `nav` and `caption_label` are hidden via `classNames` overrides so the custom header is the only nav surface.
- **Helpers split.** `resolveDateRange` and `useDateRangeLabel` live in the sibling `dateRange.ts` (not the component file) so `react-refresh/only-export-components` doesn't flag the component file. Re-exporting helpers from the component file does NOT satisfy the rule.

## Dashboard feature module
- [`features/dashboard/`](../src/features/dashboard/) — home page at `/`. Greeting title (Tashkent-aware morning/day/evening via [`lib/greeting.ts`](../src/lib/greeting.ts)), institution subtitle (placeholder until `/api/organization` lands), `<DateRangePicker>` (ZhiPay format — see "DateRangePicker" section below) URL-synced via `?range=<key>` and `&from=&to=` (only when `range=custom`); default range is `30d`.
- **Layout.** 4 KPI cards: 1-per-row on `<sm` (full-width on phones), 2×2 on `sm:`, 4 across on `lg:`. Charts row (full stack → `lg:grid-cols-3`, RevenueChart `lg:col-span-2` + PaymentStatusChart 1/3); two list panels (full stack → `lg:grid-cols-2`).
- **KPI hero typography.** `text-2xl font-semibold font-mono tabular leading-tight md:text-3xl md:leading-none` — `truncate` removed so full UZS amounts (e.g. `298 436 322 UZS`) stay readable on every viewport, wrapping at the space separators when needed. Logged in DECISIONS 2026-05-11 ("KPI hero number is `text-2xl md:text-3xl`").
- **KPIs.** Total Received (UZS + delta + 7-day area sparkline), Pending (count + students-with-debt subtitle, clicks → `/payments/pending`), Overdue (count hero + UZS subtitle in `text-destructive`, clicks → `/payments/pending?tab=overdue`), Last Payout (amount + date + next-payout relative). KpiSparkline = Recharts `AreaChart` with `stroke="hsl(var(--brand-600))"`.
- **RevenueChart.** Recharts `BarChart` with daily/weekly/monthly `Tabs` + count↔amount `Switch`. `tick.fontSize: 13`, `tickLine={false}`, `axisLine={{ stroke: 'hsl(var(--border))' }}`, bars `fill="hsl(var(--brand-600))"`. Tooltip shows formatted UZS or count via `formatUZS`/`formatNumber`. `<YAxis width={72}>` so localized compact labels like `200 млн` clear the axis area (default 60 was too narrow; the value 48 from the initial pass clipped labels).
- **PaymentStatusChart.** Recharts donut (`<Pie innerRadius=64 outerRadius=92>`) over success-600 / warning-600 / destructive cells. Center label is an absolute-positioned overlay carrying `text-2xl tabular font-mono` total. Legend renders as a `<ul>` of color-chip + label + count below the chart.
- **Recent Transactions panel.** 10 rows: avatar initials (slate-100 circle, `text-xs` per §0.2 avatar fallback allow-list) + name + `<ChannelBadge>` + relative time + amount via `<Money>` + `<StatusBadge>`. Footer: "Все платежи" + lucide `ArrowRight` → `/payments/transactions`.
- **Unpaid Students panel.** 10 rows: name + dept + amount + days-overdue in `text-destructive` (ICU plural via `daysOverdue_one/few/many/other` in `ru.json`). Footer: "Все просроченные" link + `<WriteButton variant="destructive">Отправить массовое напоминание</WriteButton>` → `<ConfirmDialog>` (`destructive` + `requireReason` + `minReasonLength={20}`) → MSW `POST /api/students/bulk-remind` → sonner toast with sent count. Reason note re-uses the global `ConfirmDialog` default after its 10 → 20 bump.
- **6 states per panel.** `PanelStates.tsx` exports `KpiCardSkeleton`, `ChartSkeleton`, `ListRowSkeleton`, `PanelEmptyState`, `PanelErrorState` (with retry), `PanelOfflineState` (no-cache), `PanelOfflineNote` (banner above stale data), `PanelPartialNote` (shown/total banner). All 5 panels render every state.
- **MSW.** [`src/mocks/handlers/dashboard.ts`](../src/mocks/handlers/dashboard.ts) seeds 240 deterministic students (mulberry32 seeded on brand-600 hex). Six endpoints: summary, revenue, status-breakdown, recent transactions, unpaid-top, bulk-remind. `?_state=partial|empty|error` query override on every GET makes 6-state coverage QA-reproducible without flipping the network.
- **Response `_meta`.** Each GET response carries optional `_meta?: { partial?, shown?, total? }`. List endpoints return `{ items, _meta }` shape; summary/revenue/status-breakdown carry `_meta` flat on the data root.
- **GreetingTitle.** `setTimeout(msUntilNextHour)` self-re-arms at each top-of-hour so the greeting flips morning, day, evening live without a reload.

## Organization feature module
- [`features/organization/`](../src/features/organization/) — `/organization/*` with 4 sub-routes for **Profile · Departments · Bank Accounts · Branding**. Nested under `<OrganizationLayout>` which renders `<PageHeader>` + `<OrgTabsNav>` + `<Outlet />`. Tabs strip is normal-flow (not sticky) and bleeds to viewport edges on mobile via `-mx-4 md:mx-0 overflow-x-auto whitespace-nowrap` so it horizontally scrolls without truncation. Tab links use `<Link to="/organization/{slug}" aria-current={isActive ? 'page' : undefined}>`.
- **Add flows are full pages, not overlays.** Two routes register **outside** the OrganizationLayout (sibling routes, no tabs): `/organization/bank-accounts/new` → [`AddBankAccountPage`](../src/features/organization/pages/AddBankAccountPage.tsx) and `/organization/departments/new[?parentId=X]` → [`AddDepartmentPage`](../src/features/organization/pages/AddDepartmentPage.tsx). Both pages follow §0.5 detail-page convention: `<BackLink to="…" pluralName="…" />` row → `text-page-title` heading → form `max-w-2xl space-y-4` → fixed-bottom action bar (Pattern A) with `md:left-[var(--sidebar-width,4rem)]` offset. Cancel + Save buttons are equal-width on mobile (`flex-1`), content-sized right-aligned at `md+`. Logged in DECISIONS 2026-05-11 ("add flows are pages").
- **Profile** (`/organization/profile`): 2-col `md:grid-cols-2`. Left = RHF + Zod factory (`nameRu/Uz/En`, `type` read-only, `tin` read-only, `legalForm` Select, `region` Select from `UZ_REGIONS`, `address` Textarea, `website` URL, `foundedYear`). Right = branding preview card (logo 128×128, primary color swatch + HEX, "Edit on Branding tab" link with `ArrowRight`) + live `<ReceiptPreview>`. Bottom: `<WriteButton>Сохранить изменения</WriteButton>`. 6 states (loading skeleton matching layout / error w/ retry / offline / partial banner / empty (N/A) / data).
- **Departments** (`/organization/departments`): two-pane on `md+`, single-pane + bottom Sheet on mobile.
  - **Tree** (left, 2fr): `useDepartments` fetches a **flat** `Department[]` and `buildTree()` derives the nested form via `useMemo`. Search Input (200ms debounce, auto-expands ancestors of matches). `<DndContext>` from `@dnd-kit/core` with `PointerSensor` activated by **`delay: 250, tolerance: 8`** (so touch scroll wins by default; long-press starts a drag — fixes mobile touch-vs-scroll conflict). `closestCorners` collision; cycle-prevention via `descendantIds(items, draggedId)` banned-set passed to each `useDroppable` as `disabled`. A separate root drop zone (`__root__` id) appears only during drag to allow dropping a node back to root. On drop → `<ConfirmDialog>` "Переместить {dept} в {newParent}?"; if `dept.studentCount > 50`, dialog requires reason ≥20.
  - **Nodes** ([`DepartmentNode.tsx`](../src/features/organization/components/DepartmentNode.tsx)): recursive render with type-aware icons (faculty/department/class/group/other). Mobile uses `h-11` (44px tap target) + `12px` per-level indent + chevron `size-9`; desktop uses `h-10` + `16px` indent + `size-7` chevron. Action icons cluster (`Pencil / Plus / Trash2`) is `hidden md:flex md:opacity-0 md:group-hover/row:opacity-100 md:focus-within:opacity-100` — mobile users open the bottom-Sheet detail panel (which provides the same actions plus a "+ Add child" button) by tapping the row.
  - **Detail panel** ([`DepartmentDetailPanel.tsx`](../src/features/organization/components/DepartmentDetailPanel.tsx)): right pane on `md+`, bottom Sheet (`h-[90dvh]` flex-col with `shrink-0` header/button + `min-h-0 flex-1` body) on mobile. Form fields: `nameRu` / `nameUz` / `type` Select / `headStaffId` (text input placeholder for now — real staff Combobox lands when `/staff` page ships) / `paymentTypes` Checkbox group (tuition / dormitory / other) / `notes` Textarea + read-only `studentCount`. Footer: Delete (left, destructive WriteButton → cascade-aware ConfirmDialog) · Cancel · Save (right, WriteButton).
  - **Cascade delete**: clicking Delete on a node with children opens `<ConfirmDialog requireReason minReasonLength={20} destructive>` with body "Удалить «X» вместе с N дочерними и M студентами? Это действие нельзя отменить." Server cascades the subtree via `descendantsOf(...)` in the MSW handler.
- **Bank Accounts** (`/organization/bank-accounts`): card list, 1 per account. [`BankAccountCard.tsx`](../src/features/organization/components/BankAccountCard.tsx) is `Card overflow-hidden` so the inner footer-border-top clips cleanly. Card body: bank logo (lucide `Building2`) + bank name + `<MaskedAccount>` + label + currency + Default badge if `isDefault` + verification `<StatusBadge>` (`paid` = Verified / `pending` = Awaiting / `failed` = Failed). Footer actions: `[По умолчанию]` (only when not default + verified) + `[Редактировать]` + `[Удалить]` (WriteButton destructive → ConfirmDialog reason ≥20).
  - **Edit** stays in a `<ResponsiveSheet>` via [`BankAccountForm.tsx`](../src/features/organization/components/BankAccountForm.tsx) (edit-only after the add-page extraction). Shows a `<BankImmutableSummary>` card listing the immutable bank/MFO/account/holder values + editable label / isDefault / currency. The summary stacks each detail in its own row (label `text-xs uppercase tracking-wider` definition-label allow-listed by §0.2 + value `text-sm font-medium`, with `tabular break-all` on MFO + account number) and each row is a tap-to-copy `<button>` — `navigator.clipboard.writeText` with a hidden-textarea + `execCommand` fallback for non-secure contexts, `navigator.vibrate?.(10)` feature-detected haptic, trailing icon swaps `Copy → Check (success-700)` for 1500ms, sonner toast + `sr-only role="status" aria-live="polite"` announcement.
  - **Add** is the standalone page at `/organization/bank-accounts/new` ([`AddBankAccountPage.tsx`](../src/features/organization/pages/AddBankAccountPage.tsx)). Uses `<BankPickerFields>` + `<CurrencyPicker>` from [`BankAccountFormParts.tsx`](../src/features/organization/components/BankAccountFormParts.tsx) (shared between page and Sheet). On submit → MSW `POST /api/organization/bank-accounts` returns `verification: 'pending'` + sets a server-side `setTimeout(5000)` that flips it to `'verified'`; client-side `useBankAccountMutations.create.onSuccess` does an optimistic cache patch + schedules `queryClient.invalidateQueries` at 5500ms to pick up the verified flip.
  - **Default-account exclusivity**: `setDefaultBankAccount` MSW endpoint sets `isDefault: true` on the target and `false` on all others. Client cache mutation does the same. `useBankAccountMutations.create.onSuccess` also unsets others if the new account is marked default.
- **Branding** (`/organization/branding`): 2-col. Left = `<LogoUploader>` (2MB max, min 256×256, PNG/SVG/JPG via `accept` prop; `<WriteButton variant="destructive">` for delete-logo → ConfirmDialog reason ≥20) + `<ColorPicker>` (default `#1558B0`, **does NOT mutate `:root`** — saved color is org-data applied to receipts/exports only) + `<Textarea>` receipt footer (max 200, char counter). Right = live `<ReceiptPreview>` driven by RHF `watch()` with `style={{ color: primaryColor }}` on the receipt header only.
- **MSW** ([`src/mocks/handlers/organization.ts`](../src/mocks/handlers/organization.ts)): 16 endpoints — `GET/PATCH /api/organization`, `GET/POST/PATCH/DELETE /api/organization/departments[/:id]`, `POST /api/organization/departments/:id/move`, `GET/POST/PATCH/DELETE /api/organization/bank-accounts[/:id]`, `POST /api/organization/bank-accounts/:id/set-default`, `GET/PATCH /api/organization/branding`. `?_state=partial|empty|error` QA override on every GET. Fixtures: 1 TATU organization, **111-node tree** (3 faculties × 4 departments × 2 years × 3 groups = 72 leaves + 39 inner nodes), 2 bank accounts (both UZS verified, one default), default branding (brand-600 + "Спасибо за оплату!" footer).
- **Shared promotions**: [`LogoUploader`](../src/components/shared/LogoUploader.tsx) / [`ColorPicker`](../src/components/shared/ColorPicker.tsx) / [`ReceiptPreview`](../src/components/shared/ReceiptPreview.tsx) moved from `features/onboarding/components/` to `src/components/shared/`. `LogoUploader` is now parameterized via a `labels: { dropHint, choose, remove, errorType, errorSize, errorDimensions? }` prop (consumers pass `t(...)` results) + `maxSizeBytes` (default 1MB) + `minDimensions?` (org branding passes `{ width: 256, height: 256 }`) + `accept` (default PNG/SVG/JPG). `ReceiptPreview` reads from a new `common.receipt.*` i18n namespace (was `onboarding.receipt.*`). Onboarding Step 2 + Organization Profile/Branding all consume from `shared/`.
- **Cross-feature imports flagged for follow-up**: Organization imports `<PanelStates>` primitives from `features/dashboard/components/PanelStates.tsx` and `<BankCombobox>` from `features/onboarding/components/BankCombobox.tsx`. Both candidate for promotion to `shared/` to keep `design-system-layers.md` import direction strict (Components → Tokens/Primitives only, no cross-feature). Logged in DECISIONS 2026-05-11.
- **Scroll/overflow plumbing** ([`ResponsiveSheet`](../src/components/shared/ResponsiveSheet.tsx) + every internal scroll wrapper): both mobile (`flex max-h-[90dvh] flex-col`) and desktop (`flex max-h-[85vh] flex-col`) branches now use `shrink-0` for header/footer and `min-h-0 flex-1 overflow-y-auto` for the middle slot. The wrapper carries `-mx-1 px-1` (outset by 4px) so the input focus ring (`ring-2 ring-offset-2`) clears the scroll container's clip line — without this, `overflow-y-auto` implicitly forces `overflow-x: auto` per CSS spec and clips the horizontal ring. Same outset pattern applied to `DepartmentDetailPanel`'s form-fields scroll wrapper and `DepartmentTree`'s tree list. `DepartmentDetailPanel`'s `<form className="flex h-full min-h-0 flex-col">` carries the `min-h-0` chain so its `flex-1 overflow-y-auto` body actually engages.

## Onboarding feature module
- [`features/onboarding/`](../src/features/onboarding/) — 5-step linear wizard for new institution accounts. Triggered when `user.onboardingComplete === false`.
- **State**: `OnboardingProvider` (React Context — local feature state, not module store per the spec note in §0.12). Carries `draft` (current values + `completedSteps[]`) and mutators. Hydrated from `GET /api/onboarding/draft` on mount.
- **Persistence**: every Continue / Save & exit calls `PATCH /api/onboarding/draft`. If `useNetworkState()` is `false`, [`useOfflineDraftQueue`](../src/features/onboarding/hooks/useOfflineDraftQueue.ts) writes to `localStorage: unipay-onboarding-draft-queue` and an effect on `online` flushes via PATCH.
- **Step guard**: `OnboardingPage` reads `:step` param and renders the matching component. If user requests a step beyond `max(completedSteps) + 1`, redirects to the highest allowed step. Step 1 is always allowed.
- **Pages**: Step 1 org info (RU/UZ name, type, legal form, TIN regex `\d{9}`, founded year, region, address, website). Step 2 contact + branding (email pre-filled from `useSession`, masked `+998 (XX) XXX-XX-XX` phone, drag-drop logo with 1MB / PNG·JPG·SVG cap, native color picker + HEX text, receipt preview live via `watch()`; mobile receipt preview opens in a bottom Sheet). Step 3 bank accounts via `useFieldArray` with searchable `BankCombobox` (cmdk + Popover) auto-filling MFO; USD radio disabled with "Скоро" tooltip; `isDefault` exclusive across array. Step 4 departments — radio for template/skip; if template, fetch from `GET /api/onboarding/templates/:type` with all 6 states (loading skeleton, empty, error+retry, offline-no-cache, offline-cached, data); editable `DepartmentTreeEditor` (rename / add child / remove). Step 5 invite staff (optional `useFieldArray`); finish path uses `<WriteButton>` + `POST /api/onboarding/complete` + `updateUser({ onboardingComplete: true })` + dynamic-imported `canvas-confetti` + sonner success toast + redirect to `/`.
- **Sidebar lock**: `OnboardingPage` calls `setOnboardingActive(true)` on mount, `false` on unmount. Sidebar reads `useAppShell().onboardingActive` and renders nav items as disabled `<span>` with `Tooltip` "Завершите настройку, чтобы продолжить" instead of `<NavLink>`.
- **Skip Setup**: a ghost "Пропустить" button lives in the `OnboardingLayout` sticky bar (right-aligned next to `StepIndicator`) on every step. Click → `ConfirmDialog` (no reason note required — not destructive in the audit sense) → same finish-path as Step 5 (`useOnboardingComplete` + `updateUser({ onboardingComplete: true })` + toast + `navigate('/')`). Lives in [`SkipSetupButton.tsx`](../src/features/onboarding/components/SkipSetupButton.tsx). Persists whatever draft the user has already saved.

## Auth feature module
- [`features/auth/`](../src/features/auth/) — sign-in, forgot-password, reset-password.
- `signIn` flow: form (RHF + Zod factory schema, `t`-aware) → `signIn(email, password)` async → MSW `POST /api/auth/sign-in` → role picked by email-prefix dev fixture or domain hint (`@admin.` / `@finance.` / `@operator.` / `@viewer.`, default `finance_manager`) → session in `sessionStorage` → redirect to `?next=` (must start with `/`) or `/`.
- **MSW runs in both dev and prod builds** — [`src/main.tsx`](../src/main.tsx) calls `worker.start({ serviceWorker: { url: \`${import.meta.env.BASE_URL}mockServiceWorker.js\` }, onUnhandledRequest: 'bypass' })` so the worker resolves under `/` (dev) and `/unipay-dashboard/` (GitHub Pages). When the real backend lands, gate this behind `VITE_USE_MOCKS` rather than reinstating the `import.meta.env.DEV` short-circuit.
- **Sign-in form pre-fills** `owner@unipay.dev` / `demo1234` as `defaultValues` in every environment — demo deploy needs the prefill, and pulling it under the DEV branch is redundant once MSW always serves the endpoint. Swap to `finance@unipay.dev` if you want the prefill to land on `/` instead of `/onboarding/1`.
- Failed-attempts lockout: 5 fails / 15-min window via [`useFailedAttempts.ts`](../src/features/auth/hooks/useFailedAttempts.ts). `LockedAlert` shows live `M:SS` countdown.
- `PasswordField` is `Input` + `Eye`/`EyeOff` toggle (36×36, deviation in DECISIONS); `autoComplete` forwards from caller (`current-password` on sign-in, `new-password` on reset).
- `DevRoleSwitcher` (DEV-only): 4 buttons, `signInAsRole(role)` bypasses MSW for fast role swapping.
- `forgot-password`: always-200 MSW (no enumeration leak); success view replaces form with `<Alert variant="success">` confirming the email.
- `reset-password`: token from `?token=`. Missing → toast + redirect. Submit → MSW (must start `valid-`). On success → success toast + redirect. On reject → error toast + redirect.
- AuthLayout has lg+ brand panel split (left: `bg-brand-600` + radial gradient + logo + tagline; right: form column with logo hidden, ThemeToggle visible).

## Staff feature module
- [`features/staff/`](../src/features/staff/) — `/staff` (list) + `/staff/:id` (detail, §0.5 Pattern B). **No Sheet variant** — list rows navigate to the full page. Folder structure: `pages/` (2 files) · `components/{list,detail,dialogs,shared}/` · `hooks/` (9 hook files including module-level sessions store) · `schemas.ts` · `api.ts`.
- **List page** ([`StaffListPage.tsx`](../src/features/staff/pages/StaffListPage.tsx)): `<PageHeader>` (title + Invite WriteButton → `<InviteStaffDialog>`) + `<StaffFilters>` (Search 200ms debounce + Role Select + Status Select, URL-synced via `?q=&role=&status=&page=`) + `<StaffTable>` (DataTable consumer using `rowHref={row => '/staff/${row.id}'}` + `getRowNavigateState={() => ({ from: location.pathname + location.search })}` so the detail's BackLink can return to the same filtered view). Pending-invite rows get a `before:absolute before:inset-y-0 before:left-0 before:w-[2px] before:bg-warning-600 before:content-[""]` accent stripe. Row kebab (5 actions): Open / Edit role / Reset password / (Deactivate \| Cancel invite per status) — Open + EditRole use `navigate(..., { state: { autoOpen: ... } })` to land on the detail page with the dialog pre-opened.
- **Detail page** ([`StaffDetailPage.tsx`](../src/features/staff/pages/StaffDetailPage.tsx)): Pattern B (kebab in header, no fixed action bar, `pb-12`). Custom `<StaffDetailHeader>` (bypasses generic `<DetailHeader>` because staff's identity row carries too much chrome): mobile-friendly layout = avatar | (title block w/ kebab pinned top-right) | (badges wrap inside title col), chips row below (registered / last login / N depts / invite-sent if pending). 4 tabs styled to match `<OrgTabsNav>` underline (NOT segment pills) — class strings in `TAB_LIST_CLASS` / `TAB_TRIGGER_CLASS` constants:
  - **Profile** = single Card containing identity header (avatar + name + email) + editable section (RHF + Zod: fullName / email / phone / locale / timezone, toggle between read/edit) + system metadata (memberId with copy-button, createdAt, lastLoginAt, createdBy → linked to inviter's profile via `useStaffById(invitedBy)`, displays as "Name [ID]"). Email editing lives here (no separate EditContactDialog).
  - **Role & Permissions** = 3 cards: role badge + description + Edit-role WriteButton; permissions matrix table (`6 resources × {read, write, destructive}` driven by `ROLE_PERMISSIONS` from domain.ts); department access chips + Edit-access WriteButton.
  - **Activity Log** = filters card (action type Select + DateRangePicker) + DataTable with all 6 states + ip/device columns. Filters compose. Reset-filters CTA in filtered-empty state.
  - **Sessions** (own profile or Owner only) = header card (title + Revoke-all-others WriteButton) + DataTable with device/browser/ip/location/lastActive + per-row Revoke WriteButton. Cross-tab sync via `useStaffSessions` storage-event listener.
- **Dialogs** in `components/dialogs/`: `EditRoleDialog` (single-screen RadioGroup w/ role descriptions + live permission diff (`success-700` added / `danger-700` removed, flat resource × capability pairs) + reason ≥20 always visible), `EditAccessDialog` (mode toggle "Full access" vs "Specific departments" + `<DepartmentTreePicker>`), `DeactivateStaffDialog` + `DeleteInviteDialog` (`<ConfirmDialog>` wrappers), `DeleteAccountDialog` (2-step: reason ≥20 → type email to confirm), `TransferOwnershipDialog` (2-step: reason ≥20 → type `TRANSFER` to confirm), `RevokeSessionDialog` + `RevokeAllOthersDialog`.
- **Cross-tab sessions sync** ([`useStaffSessions.ts`](../src/features/staff/hooks/useStaffSessions.ts)): TanStack Query + a `storage` event listener on `unipay-staff-sessions-sync` key. Revoke mutations call `bumpStaffSessionsSync()` which writes to localStorage — every other tab gets a `storage` event and invalidates its sessions query. When Prompt 10 Settings ships the shared `useMyActiveSessions(staffId?)` store, hoist this logic there.
- **Permissions matrix** ([`ROLE_PERMISSIONS`](../src/types/domain.ts) in domain.ts): static `Record<Role, StaffPermissionMatrix>` with 4 roles × 6 resources (students/payments/reports/staff/settings/audit) × `{read, write, destructive}` booleans. Powers both the matrix tab visualization and the EditRoleDialog live diff. Replace with backend permissions API when available.
- **MSW** ([`mocks/handlers/staff.ts`](../src/mocks/handlers/staff.ts)): 15 endpoints. `GET /api/staff/:id` returns `{ ...staff, permissions }`. Activity supports `?action=&from=&to=` filters. Sessions endpoint backs the Sessions tab. `DELETE /api/staff/:id` validates `{ reason ≥20, emailConfirm }`. `POST /api/staff/:id/transfer-ownership` validates `{ reason ≥20, confirmPhrase: 'TRANSFER' }`, promotes recipient, demotes current owner to finance_manager. Seed: 1 owner + 2 finance + 3 operators + 1 viewer + 2 pending invites; ~180 activity entries w/ ip+device; 10 active sessions across active members. `?_state=partial\|empty\|error` honored on every GET.
- **Domain additions** to [`src/types/domain.ts`](../src/types/domain.ts): `StaffMember` (extended w/ `locale`, `timezone`, `invitedAt`, `invitedBy`, `isOwner`), `StaffStatus` (`'active' | 'inactive' | 'pending'`), `StaffActivityAction` (15 action keys), `StaffActivityEntry` (w/ `ip`, `device`), `StaffSession`, `StaffResource`, `StaffPermission`, `StaffPermissionMatrix`, `STAFF_INVITABLE_ROLES`, `ROLE_PERMISSIONS`.

## Shared primitives added with Prompt 5
- **[`<DataTable>`](../src/components/shared/DataTable.tsx) extension** — new props: `rowHref?: (row) => string` (rows become semantic links: plain click → SPA navigate via `useNavigate()`, cmd/ctrl/shift-click → `window.open(href, '_blank')`, middle-click via `onAuxClick`, Enter/Space activates, `role="link"`, `tabIndex={0}`, `focus-visible:ring`), `getRowNavigateState?: (row) => unknown` (state to pass to navigate — used by Staff list to encode list URL into detail page's BackLink), `onRowClick?` (legacy click handler still supported), `rowClassName?: (row) => string` (per-row classes — used for pending-invite accent stripes).
- **[`<DetailPageSkeleton>`](../src/components/shared/DetailPageSkeleton.tsx)** — §0.5-shaped loading skeleton (back-link row + identity row w/ avatar + chips row + tab strip + body grid). Configurable via `avatar` / `chips` / `tabs` props. Used by `StaffDetailPage`; future detail pages should reuse.
- **[`<ConfirmDialog>`](../src/components/shared/ConfirmDialog.tsx) fix** — default Cancel + Confirm labels were `t('common.cancel')` / `t('common.confirm')` which returned the literal keys (the actual keys live at `common.actions.cancel` / `common.actions.confirm`). Affected every ConfirmDialog in the app.

## Students feature module
- [`features/students/`](../src/features/students/) — 5 routes wired in [`router.tsx`](../src/router.tsx) as siblings under `<AppShell>`: `/students` (list), `/students/new` (Add — Pattern A), `/students/import` (4-step wizard — Pattern A wrapper), `/students/schedules` (template card list), `/students/:id` (Detail — Pattern A). Folder structure: `pages/` (5 files) · `components/{list,add,profile,import,schedules,shared}/` · `hooks/` (9 files) · `schemas.ts` · `api.ts`.
- **List page** ([`StudentsListPage.tsx`](../src/features/students/pages/StudentsListPage.tsx)): `<PageHeader>` (title + Import + Add WriteButtons) + `<StudentsFilters>` (Search 200ms debounce + Department TreePicker multi via Popover + Year chips 1-6 + Payment status chips paid/partial/pending/overdue + Edu-type chips full-time/part-time/evening/remote; **collapsible on mobile via bottom Sheet** with all filters stacked, count badge on trigger button). URL-synced via `?q=&dept=A,B&year=1,2&status=paid,overdue&edu=full-time&page=` (lists join with `,`). 50-per-page pagination. `<StudentsTable>` renders DataTable with checkbox column (multi-select state lifted to page; `w-[1%]` collapse on actions + year + status + lastPayment columns via the new `ColumnMeta` API). Row hrefs to `/students/:id`; cmd/middle-click open in new tab. Pending-invite-style accent stripe not needed here (every student row is "active" or "inactive" — no special middle state). Mobile card stack via `<StudentMobileCard>`. **Bulk action bar** ([`BulkActionBar.tsx`](../src/features/students/components/list/BulkActionBar.tsx)) appears as a Pattern A fixed-bottom bar when `selectedIds.size > 0` — Remind / Export / Change-Dept / Deactivate-with-reason ≥20. Page wrapper conditionally `pb-28` only when bar is showing, otherwise `pb-8`. Row kebab → Open / Send SMS / Change Dept / Deactivate (reason ≥20).
- **Add Student** ([`AddStudentPage.tsx`](../src/features/students/pages/AddStudentPage.tsx)): standalone Pattern A page. Three sections — Personal (RHF + Zod: lastName / firstName / middleName / dob / gender radio / phone +998 mask / email), Academic (studentId with **async uniqueness check** via debounced `useCheckStudentId` mutation — Loader2/Check/X icon overlay), departmentId via `<TreePicker mode="single" leafOnly>` so the user picks a group-leaf and year is derived, educationType Select, enrollmentDate), Payment Setup (Switch: use template → `<Select>` of templates; manual → `useFieldArray` of period/type/amount/dueDate rows). Action bar = Cancel | Save-and-add-another (preserves `departmentId` / `educationType` / `enrollmentDate` on reset, clears personal + payment fields) | Save-and-close. UZS amount strings are parsed via `parseAmountToMoney('5 000 000')` that strips `\s` + NBSP (` `) and `,` → `.` before converting to tiyins bigint.
- **Student Profile** ([`StudentProfilePage.tsx`](../src/features/students/pages/StudentProfilePage.tsx)): Pattern A (back-link → identity row with avatar/name/studentId mono/status badge/dept path → chips row enrolled+endDate+lastActivity). Page wrapper `pb-28`. 2-col layout on `lg+` (40/60 = 2/3 cols): left = `<ProfileLeftColumn>` with Personal Info Card + Academic Info Card (both read-only — edit lives in the Sheet); right = `<Tabs>` with 4 underline-style triggers matching `<OrgTabsNav>` visual.
  - **Schedule tab** ([`ScheduleTab.tsx`](../src/features/students/components/profile/ScheduleTab.tsx)): DataTable with inline-editable period / type Select / amount / paid / dueDate cells via new `<InlineEditCell>` primitive (`text-sm` minimum, never `text-xs` per §0.2 even in compact density; Enter saves / Esc cancels / blur commits; saving state spinner). Recompute `remaining` server-side from `amount - paid`; derive `status` client-side from `amount` / `paid` / `dueDate` and pass with the PATCH so server stays consistent. Add-row button (creates an empty row to inline-edit) + Apply-template button → in-tab dialog that picks template + reason ≥20 + applies to this student only.
  - **Transactions tab** ([`TransactionsTab.tsx`](../src/features/students/components/profile/TransactionsTab.tsx)): DataTable with Date / Amount / Channel `<ChannelBadge>` / Status `<StatusBadge>` / Receipt-link columns. Click row → `<TransactionDetailSheet>` (`<ResponsiveSheet>` — right Sheet ≥md, bottom Sheet <md) with full breakdown (amount / commission / net / channel / status / date / Open-receipt link). Internal PaymentStatus → StatusBadge variant mapping (paid/refunded/failed/overdue/processing → exact variant; default → 'pending').
  - **Notes tab** ([`NotesTab.tsx`](../src/features/students/components/profile/NotesTab.tsx)): Card with Textarea + "Add note" WriteButton (disabled if `body.trim() < 3`); below = timeline `<Card>` per note with author avatar + name + relative timestamp + body. **Append-only** — no edit, no delete; audit-note copy line under the Textarea makes this explicit.
  - **Activity tab** ([`ActivityLogTab.tsx`](../src/features/students/components/profile/ActivityLogTab.tsx)): filters Card (action Select + DateRangePicker via `@/components/shared/DateRangePicker` + `dateRange.ts` helpers) + paginated DataTable with Actor / Action (i18n via `students.activity.actions.*`) / Change (field name + before/after strikethrough) / Date. Reset-filters CTA when filters narrow result to empty.
- **Action bar** ([`StudentDetailActionBar.tsx`](../src/features/students/components/profile/StudentDetailActionBar.tsx)): fixed-bottom Pattern A row (`fixed inset-x-0 bottom-0 z-30 border-t border-border bg-card px-4 py-3 md:left-[var(--sidebar-width,4rem)] md:px-6`). Edit (opens `<EditStudentSheet>` — RHF + Zod editProfileSchema, all personal + academic fields except studentId), Send SMS (modal with Textarea + send), Deactivate (visible when status='active', `<ConfirmDialog>` reason ≥20), Delete (Owner-only — `session.profile.role === 'owner'` else a `<Tooltip>`-gated disabled `<Button>` with copy "Только владелец может удалить запись"; on confirm reason ≥20 → mutation → navigate('/students')).
- **Import wizard** ([`ImportStudentsPage.tsx`](../src/features/students/pages/ImportStudentsPage.tsx)): internal step state, NOT a route per step. Inline step indicator (Download / Upload / Review / Confirm) with brand-600 active circle + success-600 done check. Step 1 ([`Step1Download.tsx`](../src/features/students/components/import/Step1Download.tsx)) **dynamic-imports** `xlsx` and writes a 2-sheet template (Students = 13 cols + 1 example row; Instructions = field × description × example). Triggered as Blob → `<a download>`. Step 2 ([`Step2Upload.tsx`](../src/features/students/components/import/Step2Upload.tsx)) drag-drop + Choose-file Input gated to `.xlsx` / `.csv`, 5 MB / 5 000-row max. POST `/api/students/import/parse` with FormData → returns `ImportSession` with fabricated rows (~15% have errors). Step 3 ([`Step3Review.tsx`](../src/features/students/components/import/Step3Review.tsx)) summary banner (OK count green / errors count red) + full DataTable of all rows with **per-cell inline edit** — error cells have `bg-destructive/10` + `text-sm` error message below the cell. PATCH `/api/students/import/:sessionId/row/:i` on blur/Enter. Download-error-report button generates xlsx client-side (header + raw fields + concatenated errors column). Continue button disabled when `errorCount > 0`. Step 4 ([`Step4Confirm.tsx`](../src/features/students/components/import/Step4Confirm.tsx)) shows commit summary; **requires reason ≥20 when committed count > 100** (§0.9 v2.0). On success → success card + "К списку студентов" link.
- **Schedule templates** ([`SchedulesPage.tsx`](../src/features/students/pages/SchedulesPage.tsx)): Card list (NOT a DataTable — small N), each `<TemplateCard>` shows name / type / single-amount or per-dept marker / period / dueDate / applied-count + Edit / Duplicate / Apply / Delete actions. Template form ([`TemplateForm.tsx`](../src/features/students/components/schedules/TemplateForm.tsx)) in `<ResponsiveSheet>` — name, type Select, amountMode RadioCard (Single / Per-department), amount Input (single mode only — per-dept editing deferred to a follow-up), dueDate, periodLabel, appliesTo (TreePicker multi for departments + year chips 1-6). **Live preview count** via debounced `useQuery(['students', 'template-preview', deptKey, yearKey])` calling `studentsApi.list({ departmentIds, years, pageSize: 1 })` and reading `.total`. Apply opens `<ApplyTemplateDialog>` (reason ≥20 + preview count + applies via `useApplyTemplate` mutation).
- **MSW** ([`mocks/handlers/students.ts`](../src/mocks/handlers/students.ts)): 24 endpoints. List supports `search` / `departmentId[]` (with subtree expansion through `GROUP_INDEX` ancestor walk) / `year[]` / `paymentStatus[]` / `educationType[]` / `status[]` / `page` / `pageSize`. Schedule `POST` adds a row, `PATCH /:rowId` recomputes `remaining` from `amount - paid`, `DELETE /:rowId` removes. Notes `POST` validates min length 3 (low-stakes append). Activity supports `?page=&pageSize=&action=&from=&to=`. Import `POST /parse` accepts FormData (file), fabricates 25-44 rows with ~15% deliberate errors + 1 duplicate-studentId planted for review-flow QA. `POST /commit` requires reason ≥20 only when `cleanRows.length > 100`. Templates apply enforces reason ≥20 always. Bulk endpoints (`bulk-remind` / `bulk-export` / `bulk-change-dept` / `bulk-deactivate`) wired; `bulk-deactivate` validates reason ≥20. Deterministic seed via `mulberry32(0x5740e7)` — 200+ students across 72 group leaves, schedule rows with realistic paid/partial/overdue mix, transactions tied to paid/partial rows, 3 pre-seeded templates. `?_state=partial|empty|error` honored on every GET.
- **Domain additions** to [`src/types/domain.ts`](../src/types/domain.ts): `StudentPaymentStatus = 'paid'|'partial'|'pending'|'overdue'` (distinct from transaction `PaymentStatus` which carries `processing`/`failed`/`refunded`), `PaymentType = 'tuition'|'dormitory'|'other'`, `ScheduleRow` (period / type / amount / paid / remaining / dueDate / status / templateId? / createdAt / updatedAt), `ScheduleRowStatus` (same 4 values as `StudentPaymentStatus`), `ScheduleTemplate` (name / type / amountMode 'single'|'per-department' / amount or perDepartmentAmounts[] / dueDate / periodLabel / appliesTo {departmentIds, years, studentIds} / appliedCount / createdAt / createdBy), `StudentNote` (authorId / authorName / body / createdAt), `StudentActivityAction` (15 values), `StudentActivityEntry` (action / actorId? / actorName? / field? / before? / after? / createdAt), `ImportRowError`, `ImportRow` (raw shape with all 13 columns), `ImportSession` (id / fileName / totalRows / okCount / errorCount / rows / status 'parsed'|'committing'|'committed'|'failed' / createdAt). `Student.paymentStatus` re-typed to `StudentPaymentStatus`. `Student` extended with optional `lastPaymentAt` / `createdAt` / `updatedAt`.
- **Cross-feature import**: students consumes `useDepartments` from `@/features/organization/hooks/useDepartments` (and the staff feature does the same). Data hooks treated as a 1:1 resource lift — separate from the presentation-layer import-direction rule. Flagged in DECISIONS for future promotion to `src/hooks/` if a third consumer needs it.

## Reports feature module
- [`features/reports/`](../src/features/reports/) — 2 sub-routes under `/reports/*`: **Summary · Export**. Nested under `<ReportsLayout>` which renders `<h1>` + `<ReportsTabsNav>` + `<Outlet />`. Sidebar's `/reports` NavLink still works via React Router's default prefix-active matching; the index route redirects to `summary` via `<Navigate to="summary" replace />`. Sibling pattern to Organization.
- **Shared URL range state.** `?range=&from=&to=` is the single source of truth for the active date range across both tabs. [`useReportRangeParam`](../src/features/reports/hooks/useReportRangeParam.ts) parses/serializes the triplet (default `30d`). `<ReportsTabsNav>` copies `searchParams.toString()` into the tab `<Link to>` so switching Summary↔Export preserves the range. Export form initializes its `dateRange` field from the URL and remains form-local once dirtied. Logged in DECISIONS 2026-05-11.
- **Summary** (`/reports/summary`): right-aligned `<DateRangePicker>` trigger → `<SummaryKpiRow>` (4 KPIs — Total received / Commission / Net / Payouts count, each with `formatDelta` vs previous-equal-window) → `<RevenueOverTimeChart>` full-width (Recharts `BarChart` + daily/weekly/monthly `<Tabs>`; `fill="hsl(var(--brand-600))"`, `tick.fontSize:13`) → `lg:grid-cols-2` row with `<ChannelBreakdownChart>` (horizontal `BarChart`, sorted desc, tooltip shows amount + share %) + `<DepartmentBreakdownChart>` (donut in `h-72` container, `innerRadius={80} outerRadius={120}`; slice palette via scale tokens cycling — `brand-600` / `brand-400` / `success-600` / `warning-600` / `info-600` / `slate-400`; center label uses `formatCompact` with `max-w-[9.5rem] truncate` width clamp + `text-2xl md:text-3xl` so even long UZS totals can't overflow the 160px hole; currency code lives on the smaller label line below ("Всего получено · UZS"); legend list is vertical with dept name left + amount right via `flex items-center gap-3` + `min-w-0 flex-1 truncate` + `whitespace-nowrap tabular`) → `<ByDayTable>`.
- **ByDayTable**: 6 columns (date / transactions / charged / commission / net / payout pill). Sortable. Per the column-meta lesson: date + payout columns `w-[1%] whitespace-nowrap` on BOTH header AND cell; amount/transactions columns mirror `text-right whitespace-nowrap` on both sides so values like `"116 562 100 UZS"` never break at digit-group spaces and headers like `"Дата"`/`"Транзакции"` never wrap to two lines. Pagination 30/page with `[30,60,90]` per-page selector. Mobile gets `mobileCardRender` with date+payout-pill on the top row and a 2-col `grid-cols-2` of label/value pairs below (matches the dashboard's recent-transactions visual rhythm).
- **Export** (`/reports/export`): `<ExportForm>` (RHF + Zod factory in `schemas.ts`) — fields = `dateRange` (`<DateRangePicker>` trigger; label-over-control block uses `flex flex-col gap-2` instead of `space-y-*` because shadcn's `<Label>` is inline and `margin-top`-based spacing collapses on inline elements), `dataType` (4-card `<RadioGroup>` — transactions / students / overdue / payouts; `grid-cols-1 sm:grid-cols-2 md:grid-cols-4` so cards sit in a single row on `md+`; active card gets `border-brand-600 bg-brand-50 text-brand-700`), `format` (chip-toggle pair CSV / NDJSON via `<button role="radio">` with the same brand-50 active state), `grouping` (`<Select>`: none / student / department / day), `includeContext` (`<Switch>` + hint line). Submit `<WriteButton>` is offline-auto-disabled with tooltip; while `useGenerateExport` + `useExportPolling` are working, the button shows `<Loader2>` + "Готовим экспорт…" label AND a sibling `<p role="status" aria-live="polite">` reads "Готовим экспорт… ~N сек" (ETA from server). On `ready`, an inline `<CheckCircle2>` + "Файл готов" + external-link "Открыть" appears.
- **`<RecentExportsList>`**: 6-state Card list (top = title/description; below = `<ListRowSkeleton>` × 3 on loading / `<PanelEmptyState>` empty / `<PanelErrorState onRetry>` error / `<PanelOfflineState>` offline-no-cache / `<PanelOfflineNote>` offline-with-stale / `<PanelPartialNote>` partial / data). Each item = file icon in brand-50 square + filename `tabular` + meta line (`formatDateTime` + data-type label + ICU-pluralized rows count) + inline progress note OR `<ExpiryCountdown>`. Actions cluster: `Download` `<Button>` (when ready) + `<WriteButton variant="destructive">` Delete → `<ConfirmDialog requireReason minReasonLength={20} destructive>` with `reasonPlaceholder`.
- **`<ExpiryCountdown>`**: 1-minute self-ticker via `setInterval(60_000)`. Renders "истекает через N дн./час." (ICU plural keys in both RU + UZ) when remaining > 0; "истёк" past-tense in `text-destructive` when remaining ≤ 0. Hours used below 24h, days at/above. Inline lucide `Clock` icon.
- **MSW** ([`mocks/handlers/reports.ts`](../src/mocks/handlers/reports.ts)): 6 endpoints — `GET /api/reports/summary`, `GET /api/reports/by-day`, `GET /api/exports`, `POST /api/exports`, `GET /api/exports/:id`, `DELETE /api/exports/:id`. Deterministic `mulberry32(0x1558b0)` seed (matches dashboard for visual consistency). 90-day per-day fixture pre-built at module load: each `DayBucket` carries `charged / commission / net / count / byChannel / byDepartment / payoutId?` (payouts settle on Tuesdays). All aggregations (summary / channels / departments / revenue series / by-day rows) derive from this bucket array via the `bucketsInRange(from, to)` window helper. Summary computes `deltaPct` against an equal-length previous window. Export polling endpoint auto-flips `processing → ready` 3s after the POST via a wall-clock check inside `snapshotExport`; `etaSeconds` decrements from the same elapsed-time math. 4 pre-seeded sample exports (one per data type) keep the recent list non-empty on first load. `?_state=partial|empty|error` honored on every GET.
- **Promotions to `src/components/shared/`**: [`KpiCard`](../src/components/shared/KpiCard.tsx) + [`KpiSparkline`](../src/components/shared/KpiSparkline.tsx) moved out of `features/dashboard/components/` so Reports' `<SummaryKpiRow>` can consume them without cross-feature import. Dashboard's `<KpiRow>` import was repointed in the same change. Logged in DECISIONS 2026-05-11.

## Shared primitives added with Prompt 6
- **[`<TreePicker>`](../src/components/shared/TreePicker.tsx)** — generic over `TreeItem<TMeta>[]` (flat shape with `parentId: string | null`). `mode: 'single' | 'multi'`; multi supports `subtreeToggle?: boolean` (default `true`). Optional `leafOnly` restricts single-mode selection to terminal nodes (used by Add Student so year + group are derived from a group-leaf path). Search input with ancestor auto-expand. Optional `renderMeta?: (item) => ReactNode` for trailing per-row content (e.g. student count chip). Used 4 places in Students module; will replace staff's feature-local `DepartmentTreePicker` next time that area gets touched.
- **[`<PanelStates>`](../src/components/shared/PanelStates.tsx)** — promoted from `features/dashboard/components/`. Default empty-state copy switched to `common.states.noData` (generic) — dashboard's two list panels updated to pass explicit `body={t('dashboard.empty.noData')}` to preserve their period-specific copy.
- **[`<DataTable>`](../src/components/shared/DataTable.tsx) per-column meta** — `ColumnMeta.headerClassName` / `cellClassName` exposed via TanStack module augmentation. Lets consumers collapse actions / status / numeric columns to content via `meta: { headerClassName: 'w-[1%]' }` and right-align via `meta: { cellClassName: 'text-right' }` or trim padding with `pr-3`. Applied retroactively to staff's actions column.
- **[`<StatusBadge>`](../src/components/shared/StatusBadge.tsx) partial variant** — added `'partial'` variant (`CircleDot` icon + `info` tone). Used by Student list / Schedule row status / dashboard's UnpaidStudents future polish.
- **[`<WriteButton>`](../src/components/unipay/WriteButton.tsx) i18n** — offline tooltip now reads `t('common.offline.tooltip')` instead of hardcoded RU.
- **`<InlineEditCell>`** (feature-local at [`students/components/profile/InlineEditCell.tsx`](../src/features/students/components/profile/InlineEditCell.tsx)) — click-to-edit table cell. `text-sm` minimum in every density per §0.2. Enter saves (calls `onSave(next)` promise — caller surfaces toast on failure), Esc cancels, blur commits. Pencil icon appears on hover/focus only. Promote to shared/ if a second feature (Transactions detail page?) needs it.

## Open work / not yet done
- Real backend auth (currently sessionStorage placeholder + MSW). The `lib/auth.ts` `signIn` is now async-shaped, swapping to a real backend is a fetch URL change.
- Real i18n keys for feature screens 5-12 (auth + onboarding + dashboard + organization keys done).
- Feature pages 9–10: payouts, settings — placeholders only. **Staff (Prompt 5) + Students (Prompt 6) + Payments (Prompt 7) + Reports (Prompt 8) are done.**
- Dashboard's institution subtitle (name · type) still uses a placeholder constant `УНИПЭЙ · Университет` in [`DashboardPage.tsx`](../src/features/dashboard/pages/DashboardPage.tsx) — wire to the now-available `GET /api/organization` endpoint when polishing the dashboard greeting.
- Promote `BankCombobox` from `features/onboarding/components/` to `src/components/shared/` (1 onboarding consumer + 2 organization consumers). `PanelStates` already promoted in Prompt 6.
- Schedule tab + Activity tab DataTables still horizontal-scroll on mobile (no `mobileCardRender`). Transactions tab now has card-stack render. Schedule needs an editable mobile card (inline-edit fields stacked); Activity needs a read-only mobile card. Land alongside the next dashboard / payments polish pass.
- Promote `useDepartments` data hook from `features/organization/hooks/` to `src/hooks/` once a third consumer needs it (currently staff + students cross-feature-import it).
- Apply Template per-department amount editing in `TemplateForm` is deferred — the form sets `amountMode='per-department'` but the per-dept amount inputs aren't surfaced yet. MSW handler already supports `perDepartmentAmounts[]`. Land when the first customer asks for non-uniform tuition per kafedra.
- Individual studentIds picker in `TemplateForm.appliesTo` deferred — current form lets users target by department + year only. MSW + apply endpoint already support `studentIds[]` in the body. Add a multi-select Combobox here when needed.
- Department `headStaffId` field on the Detail panel is still a plain text Input. Now that `/staff` exists, swap it for a real searchable staff Combobox (use `useStaff` to power the options).
- `useStaffSessions` is a feature-local store. When Prompt 10 Settings ships the shared `useMyActiveSessions(staffId?)` module store, hoist this logic up and have the staff feature consume the shared module. Cross-tab sync via `storage` event already works.
- `ROLE_PERMISSIONS` is a static stub. When the backend exposes a real permissions API per staff member, replace the static map and the EditRoleDialog live-diff source with the backend response.
- MSW dashboard date-range filter — handlers still accept `?range=&from=&to=` but the seed isn't date-windowed. Same for new org endpoints (`_state` overrides only). Wire when backend lands or when fixtures need it for demo polish. Reports handler **does** window on `from/to` via `bucketsInRange()` — port this to dashboard when convenient.
- Dashboard `?range=&from=&to=` filter currently ignored by MSW (handlers accept the params but the seed isn't date-windowed); wire real date filtering when the backend lands or when fixtures need it for demo polish.
- Reports recent-exports download `<a href={item.url}>` targets `/mock-downloads/{fileName}` — no real file is served (MSW doesn't generate the actual CSV/NDJSON yet). Wire either a client-side generator or a backend stream when exporting becomes real.
- Dashboard panel deep-links target placeholder routes (`/payments/transactions`, `/payments/pending`, `/payments/pending?tab=overdue`); these resolve to `<Placeholder />` today. Land when those feature pages ship.
- Chord listener implementation (`g d` etc.) — registry exists, dispatcher doesn't.
- Theme toggle wired but dark-mode token coverage not yet QA'd in every component.
- Sidebar role-aware filtering (uses `roles?: Role[]`) — registry doesn't carry roles yet.
- "Remember me" checkbox is wired visually only — no longer-lived persistence layer behind it yet.
- Onboarding: organisation `primaryColor` is captured but does NOT mutate `:root` tokens (per spec note — that lands in a later prompt). Test transfer flow note shown to user is informational only — no actual transfer fires.

## Verification status
- `npm run typecheck` — clean
- `npm run lint --max-warnings 0` — clean
- `npm run build` — clean (only chunk-size advisory)
- §0.9 audit greps — clean (every `text-xs` hit maps to allow-list)

## Discipline pointers
- All design constraints: [`STYLE_DISCIPLINE.md`](../STYLE_DISCIPLINE.md) v2.0 (§0.1 colors, §0.2 typography, §0.3 layout, §0.4 sidebar, §0.5 detail page, §0.6 tables, §0.7 mobile, §0.8 state coverage, §0.9 forbidden patterns, §0.10 acceptance, §0.11 system states, §0.12 module-level state stores).
- Deviations log: [`docs/DECISIONS.md`](../docs/DECISIONS.md).
- Surface status table: [`docs/product_states.md`](../docs/product_states.md).
- This snapshot can decay — check git history before trusting it on tech-stack questions.
